{
  "name": "report-1145-xml-converter",
  "version": "1.0.2",
  "description": "Convert Report 1145 Excel files into FUTURELOG XML format",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@cloudflare/vite-plugin": "^1.0.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
